# ── ASR Dashboard Frontend — Environment Variables ─────────────────────────
# Copy this file to .env.local for local dev, or set in Vercel dashboard.

# URL of the deployed FastAPI backend.
# Leave empty for local dev (Vite proxy handles /api automatically).
# Vercel example: https://asr-dashboard-backend.vercel.app
VITE_API_URL=
